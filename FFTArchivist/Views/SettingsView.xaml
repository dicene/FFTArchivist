<Page
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FFTArchivist"
      xmlns:Entries="clr-namespace:FFTArchivist.Entries" x:Class="FFTArchivist.SettingsView"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SettingsView" Background="{DynamicResource WindowBackgroundBrush}">

    <Page.DataContext>
        <Entries:SettingsViewModel x:Name="SettingsViewModel"/>
    </Page.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Column="0" Grid.ColumnSpan="6" Grid.Row="0">
            <Label Content="FFT IVC Root Path:" VerticalAlignment="Center" Margin="5,0,0,0" DockPanel.Dock="Left"/>
            <TextBox x:Name="FFTIVCRootPathTextBox" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="6" Text="{Binding FFTIVCRootPath}" Margin="5,0,0,0" VerticalAlignment="Center" DockPanel.Dock="Right"/>
        </DockPanel>
    </Grid>
</Page>
